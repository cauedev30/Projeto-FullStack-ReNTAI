{
  "name": "rentai-teleconsultoria",
  "version": "0.1.0",
  "private": true,
  "description": "Modulo fullstack de teleconsultoria com validacao inteligente de documentos para o desafio ReNTAI P01.",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "test:e2e": "npm run build && playwright test",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "setup:env": "node scripts/setup-env.mjs",
    "setup": "npm install && npm run setup:env && npm run db:migrate && npm run db:seed"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
